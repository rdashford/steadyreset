.claude/state/
*.xcodeproj/
DerivedData/
.DS_Store
Backend/.env*
.build/
.swiftpm/

# Generated by `xcodegen generate` from project.yml. Never edit or commit these:
# xcodegen overwrites them, so a hand edit is silently lost on the next generate.
# Change the target's `info:` / `entitlements:` properties in project.yml instead.
App/**/Info.plist
App/**/*.entitlements
